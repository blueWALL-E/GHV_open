# 高超声速飞行器姿态跟踪自适应滑模控制算法构建说明

## 1. 研究对象与控制目标

高超声速飞行器在飞行过程中存在显著的非线性、强耦合、参数不确定性和外部扰动。由于飞行高度和马赫数变化范围大，动压及气动参数会随飞行环境发生明显变化，导致飞行器动力学特性在不同飞行条件下存在显著差异。

为便于后续控制律设计，将飞行器姿态动力学整理为非线性、非仿射、含不确定项的系统形式：

$$
\dot{\Omega}(t)=F(x(t))-G(x(t),u(t))+\Delta(x(t),t)
$$

其中：

$$
x(t)=\left[\Omega^T,\omega^T\right]^T
$$

$$
\Omega(t)=\left[\mu,\alpha,\beta\right]^T
$$

$$
\omega(t)=\left[p,q,r\right]^T
$$

各变量含义如下：

- $\Omega(t)$ 为三通道姿态向量，包括滚转角 $\mu$、攻角 $\alpha$ 和侧滑角 $\beta$；
- $\omega(t)$ 为三通道角速度向量，包括 $p,q,r$；
- $F(x(t))$ 表示系统非线性函数向量；
- $G(x(t),u(t))$ 表示系统非线性、非仿射控制函数向量；
- $u(t)=[\delta_a,\delta_e,\delta_r]^T$ 表示系统控制输入向量；
- $\Delta(x(t),t)$ 表示系统不确定项，由未建模动态和外部干扰组成。

本文控制目标是使飞行器姿态 $\Omega(t)$ 能够稳定跟踪期望姿态 $\Omega_d(t)$，并在存在模型不确定性和扰动的情况下保持良好的鲁棒性。

## 2. 姿态跟踪误差定义

定义姿态跟踪误差为：

$$
e(t)=\Omega_d(t)-\Omega(t)=\left[e_\mu,e_\alpha,e_\beta\right]^T
$$

其中：

$$
\Omega_d(t)=\left[\mu_d,\alpha_d,\beta_d\right]^T
$$

$e_\mu$、$e_\alpha$、$e_\beta$ 分别表示滚转角、攻角和侧滑角的跟踪误差。

在控制器实现中，输入量包括：

$$
\ddot{\Omega}_d,
\quad e,
\quad \dot e,
\quad \int e\,dt,
\quad \rho
$$

对应 MATLAB 函数中的变量为：

| 理论变量          | 程序变量        | 含义                 |
| ----------------- | --------------- | -------------------- |
| $\ddot{\Omega}_d$ | `dd_aero_ang_d` | 期望姿态角二阶导向量 |
| $e$               | `aero_ang_e`    | 姿态角误差向量       |
| $\dot e$          | `d_aero_ang_e`  | 姿态角误差一阶导向量 |
| $\int e\,dt$      | `i_aero_ang_e`  | 姿态角误差积分向量   |
| $\rho$            | `rho_smc`       | 自适应鲁棒增益向量   |

## 3. 系统重构与控制设计思路

为便于自适应滑模控制律设计，将系统写成如下形式：

$$
\ddot{\Omega}(t)=F(x(t))+u(t)+\tilde{G}(x(t),u(t))+\Delta(x(t),t)
$$

其中将原非仿射控制项重新整理为：

$$
G(x(t),u(t))=\tilde{G}(x(t),u(t))+u(t)
$$

这样处理后，控制输入 $u(t)$ 能够显式出现在系统方程中，而剩余的非仿射影响、未建模动态及扰动可统一视为等效不确定项。该处理方式的核心目的是将复杂非仿射控制问题转化为便于滑模控制设计的等效形式。

本文采用以下假设：

1. 存在正常数 $\bar d$ 和 $\bar l$，使得不确定项和非仿射剩余项有界：

$$
\|\Delta(x(t),t)\|_\infty \le \bar d,
\quad
\|\tilde{G}(x(t),u(t))\|_\infty \le \bar l
$$

2. 期望姿态 $\Omega_d$ 及其一阶、二阶导数有界。

为统一表示不确定性上界，定义：

$$
\rho=\bar d+\bar l
$$

在实际控制器中，$\rho$ 不要求提前精确已知，而是通过自适应律在线估计。

## 4. 滑模面的构建

为了降低跟踪误差，针对每一个姿态通道构造积分型滑模面：

$$
s_\Omega(t)=\dot e_\Omega(t)+\lambda_{p,\Omega}e_\Omega(t)+\lambda_{I,\Omega}\int_0^t e_\Omega(\tau)d\tau,
\quad \Omega\in\{\mu,\alpha,\beta\}
$$

其中：

- $\lambda_{p,\Omega}>0$ 为比例误差权重；
- $\lambda_{I,\Omega}>0$ 为积分误差权重；
- $s_\Omega$ 为对应通道滑模面。

程序中三通道滑模面计算为：

```matlab
S_mu = d_mu_e + lambad_p_mu * mu_e + lambad_I_mu * i_mu_e;
S_alpha = d_alpha_e + lambad_p_alpha * alpha_e + lambad_I_alpha * i_alpha_e;
S_beta = d_beta_e + lambad_p_beta * beta_e + lambad_I_beta * i_beta_e;
```

因此，控制器不是只根据瞬时误差进行调节，而是同时利用误差、误差变化率和误差积分。积分项用于削弱稳态误差，比例项和微分项用于改善动态响应。

## 5. 控制律构建

对滑模面求导可得：

$$
\dot{s}_\Omega(t)
=\ddot e_\Omega(t)
+\lambda_{p,\Omega}\dot e_\Omega(t)
+\lambda_{I,\Omega}e_\Omega(t)
$$

结合误差定义 $e_\Omega=\Omega_d-\Omega$，控制律被设计为等效控制项和鲁棒控制项两部分：

$$
u_\Omega=u_{eq,\Omega}+u_{ro,\Omega}
$$

### 5.1 等效控制项

等效控制项用于补偿期望姿态二阶导和滑模面的确定性动态：

$$
u_{eq,\Omega}
=
\ddot{\Omega}_{d,\Omega}
+\lambda_{p,\Omega}\dot e_\Omega
+\lambda_{I,\Omega}e_\Omega
+k_\Omega s_\Omega
$$

其中 $k_\Omega>0$ 为趋近律增益，用于增强滑模面的收敛速度。

程序中对应实现为：

```matlab
u_eq_mu = dd_mu_d + lambad_p_mu * d_mu_e + lambad_I_mu * mu_e + k_mu * S_mu;
u_eq_alpha = dd_alpha_d + lambad_p_alpha * d_alpha_e + lambad_I_alpha * alpha_e + k_alpha * S_alpha;
u_eq_beta = dd_beta_d + lambad_p_beta * d_beta_e + lambad_I_beta * beta_e + k_beta * S_beta;
```

### 5.2 鲁棒控制项

为了抑制不确定项和扰动，设计鲁棒控制项：

$$
u_{ro,\Omega}=\hat\rho_\Omega\tanh\left(\frac{s_\Omega}{\varepsilon_\Omega}\right)
$$

其中：

- $\hat\rho_\Omega$ 为不确定性上界的在线估计值；
- $\varepsilon_\Omega>0$ 为滑模面边界层厚度；
- $\tanh(\cdot)$ 用于替代符号函数，降低传统滑模控制中的抖振。

程序中对应实现为：

```matlab
u_ro_mu = rho_mu * tanh(S_mu / epsilon_mu);
u_ro_alpha = rho_alpha * tanh(S_alpha / epsilon_alpha);
u_ro_beta = rho_beta * tanh(S_beta / epsilon_beta);
```

### 5.3 总控制律

三通道理论控制律为：

$$
u_\mu=u_{eq,\mu}+u_{ro,\mu}
$$

$$
u_\alpha=u_{eq,\alpha}+u_{ro,\alpha}
$$

$$
u_\beta=u_{eq,\beta}+u_{ro,\beta}
$$

程序中对应为：

```matlab
u_mu = u_eq_mu + u_ro_mu;
u_alpha = u_eq_alpha + u_ro_alpha;
u_beta = u_eq_beta + u_ro_beta;
```

需要注意的是，当前 `.m` 文件中将 `u_mu` 和 `u_beta` 后续置零：

```matlab
u_mu = 0;
u_beta = 0;
```

因此当前代码实际启用的是以攻角 $\alpha$ 通道为主的控制输出，滚转通道和侧滑通道控制量被人为关闭。这与文件头部“俯仰通道自适应滑模控制”的说明一致。

## 6. 自适应律构建

为了避免依赖不确定项上界的精确先验值，引入自适应律在线更新 $\hat\rho_\Omega$。根据引理：

$$
|s|-s\tanh\left(\frac{s}{\varepsilon}\right)<0.2785\varepsilon
$$

设计自适应律为：

$$
\dot{\hat\rho}_\Omega
=\gamma_{\rho,\Omega}
\left(
|s_\Omega|-0.2785\varepsilon_\Omega-a_\Omega\hat\rho_\Omega
\right)
$$

其中：

- $\gamma_{\rho,\Omega}>0$ 为自适应调节增益；
- $a_\Omega>0$ 为修正项系数，用于避免自适应参数无界增长；
- $0.2785\varepsilon_\Omega$ 来自双曲正切函数逼近符号函数时的上界估计。

程序中对应实现为：

```matlab
d_rho_smc_mu = gamma_rho_mu * (abs(S_mu) - 0.2785 * epsilon_mu - a_mu * rho_mu);
d_rho_smc_alpha = gamma_rho_alpha * (abs(S_alpha) - 0.2785 * epsilon_alpha - a_alpha * rho_alpha);
d_rho_smc_beta = gamma_rho_beta * (abs(S_beta) - 0.2785 * epsilon_beta - a_beta * rho_beta);
```

该自适应律的作用是：当滑模面误差较大时，提高鲁棒控制增益；当滑模面误差较小时，通过 $a_\Omega\hat\rho_\Omega$ 项抑制增益继续增大，从而兼顾鲁棒性和控制平滑性。

## 7. 舵面分配与限幅

控制器最终将虚拟控制量转换为舵面偏转：

$$
LE=u_\alpha-u_\mu
$$

$$
RE=u_\alpha+u_\mu
$$

$$
RUD=u_\beta
$$

其中：

- `LE` 为左舵偏转角；
- `RE` 为右舵偏转角；
- `RUD` 为方向舵偏转角。

由于当前程序中 `u_mu = 0`、`u_beta = 0`，实际输出为：

$$
LE=u_\alpha,
\quad
RE=u_\alpha,
\quad
RUD=0
$$

这说明当前控制器主要通过左右舵同向偏转实现俯仰通道，即攻角 $\alpha$ 通道控制。

为满足舵机物理约束，程序对三个舵面输出进行了 $\pm 30^\circ$ 限幅：

```matlab
if LE > 30
    LE = 30;
elseif LE < -30
    LE = -30;
end

if RE > 30
    RE = 30;
elseif RE < -30
    RE = -30;
end

if RUD > 30
    RUD = 30;
elseif RUD < -30
    RUD = -30;
end
```

## 8. 控制算法整体流程

该自适应滑模控制算法的构建流程如下：

1. 建立高超声速飞行器含不确定项的非线性、非仿射姿态动力学模型；
2. 将非仿射控制项拆分为显式控制输入和剩余非线性项，便于控制律设计；
3. 定义姿态跟踪误差 $e=\Omega_d-\Omega$；
4. 针对 $\mu$、$\alpha$、$\beta$ 三个通道分别构造积分型滑模面；
5. 设计等效控制项，用于补偿期望轨迹动态和误差动态；
6. 设计基于 $\tanh(\cdot)$ 的鲁棒控制项，用于抑制不确定项并减小抖振；
7. 设计自适应律在线估计不确定性上界，避免依赖固定先验边界；
8. 将虚拟控制量转换为舵面输出，并进行物理限幅。

## 9. 当前 `.m` 文件实现特点

当前 `adaptive_sliding_mode_controller.m` 的主要特点如下：

- 函数输入覆盖期望姿态二阶导、误差、误差导数、误差积分、自适应权重和三通道控制参数；
- 三通道均构造了滑模面和自适应律；
- 控制参数按通道分别设置，包括 $\lambda_p$、$\lambda_I$、$k$、$\varepsilon$ 和 $\gamma_\rho$；
- 代码中实际启用了攻角 $\alpha$ 通道控制；
- 滚转通道 $u_\mu$ 和侧滑通道 $u_\beta$ 被置零；
- 舵面输出经过 $\pm30^\circ$ 限幅。

另需注意：当前代码中 `d_rho_smc` 和 `S` 的赋值语句缩进在 `elseif RUD < -30` 分支内部：

```matlab
elseif RUD < -30
    RUD = -30;

    d_rho_smc = [d_rho_smc_mu; d_rho_smc_alpha; d_rho_smc_beta];
    S = [S_mu; S_alpha; S_beta];
end
```

这意味着只有当 `RUD < -30` 时，`d_rho_smc` 和 `S` 才会被更新；其他情况下它们保持初始化的零向量。若期望每个控制周期都输出自适应律和滑模面，应将这两行移到 `if RUD ... end` 结构之后。

## 10. 可用于论文或报告中的控制算法描述

本文针对高超声速飞行器姿态跟踪控制问题，设计了一种自适应滑模控制律。首先，将飞行器非线性非仿射姿态动力学整理为含显式控制输入和集中不确定项的等效形式。随后，定义期望姿态与实际姿态之间的跟踪误差，并针对滚转角、攻角和侧滑角三个通道分别构造含比例项、微分项和积分项的滑模面。基于滑模面动态，控制律由等效控制项和鲁棒控制项组成，其中等效控制项用于补偿期望轨迹及误差动态，鲁棒控制项采用双曲正切函数形式以削弱滑模抖振。为避免依赖不确定项上界的精确先验信息，引入自适应律在线更新鲁棒增益，使控制器能够根据滑模面误差大小自动调整补偿强度。最后，将虚拟控制量映射为左右舵和方向舵偏转，并通过限幅环节满足执行机构物理约束。当前程序实现中主要启用攻角通道控制，即通过左右舵同向偏转完成俯仰姿态跟踪。
