# 高超声速飞行器姿态控制算法1—自适应滑模控制

## 摘要

## GHV动力学建模与基本假设

&emsp;&emsp;GHV存在显著的强耦合、强非线性、快时变、高不确定性的[动力学模型](./docs/Hypersonic%20vehicle%20simulation%20model%20Winged-cone%20configuration.pdf)且具有复杂的[气动特性](./docs/Development%20of%20an%20Aerodynamic%20Database%20for%20a%20generic%20Hypersonic%20Air%20Vehicle.pdf)，且非仿射项将显著影响GHV的动力学特性。因此，为实现GHV的高性能控制，非仿射项不应被简单忽略，则可以将GHV的姿态动力学方程表示为如下MIMO的非线性非仿射系统：

$$
\ddot{\Omega}(t)=F(x(t))+G(x(t),u(t))+\Delta(x(t),t)
$$

$$
x(t)=[\Omega(t)^T,\omega(t)^T]^T
；
\Omega(t)=[\mu,\alpha,\beta]^T
；
\omega(t)=[p,q,r]^T
$$

其中：

> $\Omega(t)$ 为GHV三通道姿态向量，其中$\mu$为滚转角、$\alpha$为攻角、$\beta$为侧滑角  
> $\omega(t)$ 为GHV机体坐标系下三轴角速度向量，其中$p$为滚转角速度、$q$为俯仰角速度、$r$为偏航角速度  
> $x(t)$ 为GHV状态向量  
> $u(t)=[\delta_a,\delta_e,\delta_r]^T$ 表示系统控制输入向量,其中$\delta_a$为右升降副翼偏转角、$\delta_e$为左升降副翼偏转角、$\delta_r$为方向舵偏转角  
> $F(x(t))$ 为GHV非线性函数向量  
> $G(x(t),u(t))$ 为GHV非线性、非仿射控制函数向量  
> $\Delta(x(t),t)$ 表示系统不确定项，由未建模动态和外部干扰组成  

&emsp;&emsp;为便于自适应滑模控制律设计，将GHV动力学模型改写成如下形式：

$$
\boxed{
\begin{gathered}
\ddot{\Omega}(t)=F(x(t))+\tilde{G}(x(t),u(t))+u(t)+\Delta(x(t),t)
\\
G(x(t),u(t))=\tilde{G}(x(t),u(t))+u(t)
\end{gathered}
}
$$

&emsp;&emsp;这样处理后，控制输入 $u(t)$ 能够显式出现在系统方程中，而剩余的非仿射影响、未建模动态及扰动可统一视为等效不确定项。该处理方式的核心目的是将复杂非仿射控制问题转化为便于滑模控制设计的等效形式。

&emsp;&emsp;同时，对GHV动力学模型和期望指令做出如下假设：

&emsp;&emsp;**假设1 GHV的不确定项和非仿射项均有界** 存在正常数 $\bar d$ 和 $\bar l$，使得：

$$
\|\Delta(x(t),t)\|_\infty \le \bar d,

\|\tilde{G}(x(t),u(t))\|_\infty \le \bar l
\\
\rho=\bar d+\bar l
$$

&emsp;&emsp;**假设2 期望指令二阶可导** 期望指令$\Omega_d(t)=[\mu_d,\alpha_d,\beta_d]^T$（$\mu_d$为滚转期望指令、$\alpha_d$为攻角期望指令、$\beta_d$为侧滑角期望指令）满足如下条件：

$$
\Omega_d(t) \in C^2,\quad
\Omega_d(t),\ \dot{\Omega}_d(t),\ \ddot{\Omega}_d(t) \in L_\infty
$$

## 控制器设计

### 误差定义

&emsp;&emsp;定义姿态跟踪误差为：

$$
e(t)=\Omega_d(t)-\Omega(t)=\left[e_\mu,e_\alpha,e_\beta\right]^T
$$

其中：

> $e_\mu$、$e_\alpha$、$e_\beta$ 分别表示滚转角、攻角和侧滑角的跟踪误差。

### 滑模面设计

&emsp;&emsp;为了降低跟踪误差，针对每一个姿态通道构造如下积分型滑模面：

$$
s_\Omega(t)=\dot e_\Omega(t)+\lambda_{p,\Omega}e_\Omega(t)+\lambda_{I,\Omega}\int_0^t e_\Omega(\tau)d\tau,
\quad \Omega\in\{\mu,\alpha,\beta\}
$$

其中：

> $\lambda_{p,\Omega}>0$ 为$\Omega$通道比例误差权重
> $\lambda_{I,\Omega}>0$ 为$\Omega$通道积分误差权重

### 控制律设计

&emsp;&emsp;对滑模面两边同时求导可得：

$$
\begin{aligned}
\dot{s}_\Omega(t)
&= \ddot e_\Omega(t)+\lambda_{p,\Omega}\dot e_\Omega(t)+\lambda_{I,\Omega}e_\Omega(t)
\\
&= \ddot{\Omega}_d(t)+\lambda_{p,\Omega}\dot e_\Omega(t)+\lambda_{I,\Omega}e_\Omega(t)-F_{\Omega}-\tilde{G}_{\Omega}-\Delta_{\Omega}-u_{\Omega}
\end{aligned}
$$

&emsp;&emsp;趋近律可以设计为：

&emsp;&emsp;则控制律可以表示为：

$$
\boxed{
\begin{aligned}
u_{\Omega}
&= u_{eq,\Omega}+u_{ro,\Omega}
\\
u_{eq,\Omega}
&= \ddot{\Omega}_{d}+ \lambda_{p,\Omega} \dot{e}_{\Omega}(t)+ \lambda_{I,\Omega} e_{\Omega}(t)- F_{\Omega}+ k_{\Omega} s_{\Omega}
\\
u_{ro,\Omega}
&= \hat{\rho}_{\Omega}\tanh(\frac{s_{\Omega}}{\varepsilon_{\Omega}})
\\
\dot{\hat{\rho}}_{\Omega}
&=\gamma_{\rho_{\Omega}}(|s_{\Omega}|-0.2785\varepsilon_{\Omega}-a_{\Omega}\hat{\rho}_{\Omega})
\end{aligned}
}
$$

其中：

> $k_{\Omega}>0$ 为等效滑模控制参数
> $\varepsilon_{\Omega}>0$ 为滑模面宽度
> $\hat{\rho}_{\Omega}$ 为 $\rho_{\Omega}$ 的估计值
> $\gamma_{\rho_{\Omega}}>0$ 为自适应调节参数
> $a_{\Omega}>0$ 为修正项

### 控制分配与限幅度

控制器最终将虚拟控制量转换为舵面偏转：

$$
\begin{aligned}
\delta_a &= \operatorname{sat}_{[-30^\circ,\,30^\circ]}(u_\alpha+u_\mu) \\
\delta_e &= \operatorname{sat}_{[-30^\circ,\,30^\circ]}(u_\alpha-u_\mu) \\
\delta_r &= \operatorname{sat}_{[-30^\circ,\,30^\circ]}(u_\beta)
\end{aligned}
\\
\operatorname{sat}_{[-30^\circ,\,30^\circ]}(x)
=
\begin{cases}
30^\circ, & x>30^\circ \\
x, & -30^\circ \le x \le 30^\circ \\
-30^\circ, & x<-30^\circ
\end{cases}
$$

## 稳定性证明

## 仿真结果
